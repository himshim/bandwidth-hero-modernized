{
  "name": "bandwidth-hero-modernized",
  "version": "0.0.8",
  "description": "Modernized Bandwidth Hero extension for Chrome (Manifest V3)",
  "scripts": {
    "build": "node scripts/build.js",
    "zip": "cd dist && bestzip ../bandwidth-hero-v$npm_package_version.zip .",
    "release": "node scripts/release.js"
  },
  "devDependencies": {
    "bestzip": "^2.2.0",
    "fs-extra": "^10.0.0"
  }
}